# Pen (Plaintext Editing Notepad)

A small, fast plaintext editor written in C using raylib.

## Build (Linux)

Requirements:
- raylib (installed)
- gcc / clang
- pkg-config

Build:
```bash
make
./pen

